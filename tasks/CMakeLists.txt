# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(pose_estimationTaskLib)
ADD_LIBRARY(${POSE_ESTIMATION_TASKLIB_NAME} SHARED 
    ${POSE_ESTIMATION_TASKLIB_SOURCES})
add_dependencies(${POSE_ESTIMATION_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${POSE_ESTIMATION_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${POSE_ESTIMATION_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${POSE_ESTIMATION_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${POSE_ESTIMATION_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${POSE_ESTIMATION_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${POSE_ESTIMATION_TASKLIB_HEADERS}
    DESTINATION include/orocos/pose_estimation)

